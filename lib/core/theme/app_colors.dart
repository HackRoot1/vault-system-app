import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const scaffoldBackground = Color(0xFF0D1B2A);
  static const scaffoldBg = scaffoldBackground;
  static const cardBackground = Color(0xFF112240);
  static const inputBackground = Color(0xFF0D1B2A);
  static const primaryText = Color(0xFFFFFFFF);
  static const secondaryText = Color(0xFF8899AA);
  static const loginButtonBackground = Color(0xFFC8D8E8);
  static const loginButtonText = Color(0xFF0D1B2A);
  static const statusDot = Color(0xFF00FF88);
  static const iconColor = Color(0xFF8899AA);

  static final cardBorder = Colors.white.withValues(alpha: 0.08);
  static final inputBorder = Colors.white.withValues(alpha: 0.12);
}
